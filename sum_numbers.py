def sum_n_numbers(*args):
    """Sum n numbers passed as arguments."""
    if not args:
        return 0
    return sum(args)

def sum_list(numbers):
    """Sum a list of numbers."""
    if not numbers:
        return 0
    return sum(numbers)